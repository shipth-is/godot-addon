## Enums for Android wizard

class_name AndroidEnums

enum Platform { ANDROID, IOS, WEB }

enum CredentialsType { CERTIFICATE, KEY }

enum BuildType { AAB, APK }

enum KeyTestStatus { SUCCESS, ERROR }

enum KeyTestError { NOT_INVITED, OTHER }

enum StepStatus { FAILURE, PENDING, RUNNING, SUCCESS, WARN }
